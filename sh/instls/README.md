# instls

Inventory installed apps, brew, npm and pip as Markdown and HTML.

## Install

```bash
mkdir -p ~/.local/bin && curl -fsSL https://raw.githubusercontent.com/quixtop/quix/main/sh/instls/instls -o ~/.local/bin/instls && chmod +x ~/.local/bin/instls
```

## Usage

Run `instls -h`.

## Source

`sh/instls` in [quixtop/quix](https://github.com/quixtop/quix) · author: shrix
